# Development, test, and docs tooling (not installed in the runtime image)
-r requirements.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
# Async driver for the throwaway sqlite DB that conftest points DATABASE_URL
# at. Production uses postgres/asyncpg, so this is test-only.
aiosqlite>=0.19.0

black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.4.0

mkdocs>=1.6.0
mkdocs-material>=9.5.0
