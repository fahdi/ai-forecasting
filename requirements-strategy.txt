# Freqtrade strategy tests (tests/test_ensemble_strategy.py) only.
#
# freqtrade pins pandas/numpy differently from the app, which is why it lives
# in its own venv locally (.venv-freqtrade) and its own CI job rather than
# joining requirements-dev.txt.
#
# TA-Lib is deliberately absent: the strategy does not use it and
# `freqtrade.strategy` imports without it, so CI needs no system packages.

freqtrade==2026.6
pytest>=7.4.0

# freqtrade.data.metrics does `from scipy import stats` but freqtrade does not
# declare scipy, so `from freqtrade.strategy import IStrategy` fails on a
# clean install. It is present in .venv-freqtrade only by accident of what
# else was installed there.
scipy>=1.11
