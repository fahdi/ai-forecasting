[pytest]
# The suite imports `app.*`, which only resolved because `python -m pytest`
# happens to put the CWD on sys.path. A bare `pytest` (what CI runs) does not,
# so pin the import root here instead of depending on the invocation style.
pythonpath = .
testpaths = tests
